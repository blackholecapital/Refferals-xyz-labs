# MktMaker Referral V-1

Standalone referral link generator + admin tracking panel (localStorage demo).

## Run
```bash
npm install
npm run dev
```

## Env
Create `.env`:
```bash
VITE_WC_PROJECT_ID=YOUR_WALLETCONNECT_PROJECT_ID
```

## Cloudflare Pages
Includes `public/_redirects` for SPA routing.
